name: Next.js Version Check

on:
  schedule:
    # Run daily at 00:00 UTC
    - cron: "0 0 * * *"
  workflow_dispatch: # Allow manual triggering

jobs:
  check-nextjs-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    outputs:
      needs_update: ${{ steps.check_version.outputs.needs_update }}
      latest_version: ${{ steps.check_version.outputs.latest_version }}
      latest_major_minor: ${{ steps.check_version.outputs.latest_major_minor }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Check for new Next.js version
        id: check_version
        run: |
          # Get current package version
          CURRENT_PKG_VERSION=$(node -p "require('./packages/cache-handler/package.json').version")
          echo "Current package version: $CURRENT_PKG_VERSION"

          # Get latest Next.js version from npm
          LATEST_VERSION=$(npm view next version)
          echo "Latest Next.js version: $LATEST_VERSION"

          # Extract major.minor from versions
          CURRENT_MAJOR_MINOR=$(echo $CURRENT_PKG_VERSION | sed -E 's/^([0-9]+\.[0-9]+).*/\1/')
          LATEST_MAJOR_MINOR=$(echo $LATEST_VERSION | sed -E 's/^([0-9]+\.[0-9]+).*/\1/')

          echo "current_pkg_version=$CURRENT_PKG_VERSION" >> $GITHUB_OUTPUT
          echo "latest_version=$LATEST_VERSION" >> $GITHUB_OUTPUT
          echo "current_major_minor=$CURRENT_MAJOR_MINOR" >> $GITHUB_OUTPUT
          echo "latest_major_minor=$LATEST_MAJOR_MINOR" >> $GITHUB_OUTPUT

          if [ "$CURRENT_MAJOR_MINOR" != "$LATEST_MAJOR_MINOR" ]; then
            echo "needs_update=true" >> $GITHUB_OUTPUT
            echo "New Next.js major.minor available: $LATEST_MAJOR_MINOR (current: $CURRENT_MAJOR_MINOR)"
          else
            echo "needs_update=false" >> $GITHUB_OUTPUT
            echo "Already on latest major.minor: $CURRENT_MAJOR_MINOR"
          fi

      - name: Check if branch already exists
        if: steps.check_version.outputs.needs_update == 'true'
        id: branch_check
        run: |
          BRANCH_NAME="nextjs-${{ steps.check_version.outputs.latest_major_minor }}"
          if git ls-remote --heads origin $BRANCH_NAME | grep -q $BRANCH_NAME; then
            echo "branch_exists=true" >> $GITHUB_OUTPUT
            echo "Branch $BRANCH_NAME already exists"
          else
            echo "branch_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create feature branch
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b nextjs-${{ steps.check_version.outputs.latest_major_minor }}

      - name: Update Next.js version
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          # Update e2e-test-app
          cd apps/e2e-test-app
          pnpm add next@${{ steps.check_version.outputs.latest_version }}
          cd ../..

          # Update legacy-cache-test if it exists
          if [ -d "apps/legacy-cache-test" ]; then
            cd apps/legacy-cache-test
            pnpm add next@${{ steps.check_version.outputs.latest_version }}
            cd ../..
          fi

          # Update package versions to match Next.js
          NEW_PKG_VERSION="${{ steps.check_version.outputs.latest_major_minor }}.0"

          # Update core package
          cd packages/cache-handler
          npm version $NEW_PKG_VERSION --no-git-tag-version --allow-same-version
          cd ../..

      - name: Install updated dependencies
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: pnpm install

      - name: Run formatting
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: pnpm format

      - name: Run linting
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: pnpm lint
        continue-on-error: true

      - name: Run type checking
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: pnpm typecheck
        continue-on-error: true

      - name: Build packages
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: pnpm build

      - name: Start Docker services
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          docker compose up -d
          sleep 5
          docker ps

      - name: Install Playwright browsers
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          cd apps/e2e-test-app
          pnpm exec playwright install chromium

      - name: Run e2e tests with memory cache
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          cd apps/e2e-test-app
          pnpm build
          pnpm test:e2e
        continue-on-error: true
        id: test_memory

      - name: Run e2e tests with Redis
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          # Flush Redis
          docker exec nextjs-cache-redis redis-cli FLUSHALL

          # Build and test with Redis
          cd apps/e2e-test-app
          CACHE_HANDLER=redis pnpm build
          CACHE_HANDLER=redis pnpm test:e2e
        continue-on-error: true
        id: test_redis

      - name: Stop Docker services
        if: always()
        run: docker compose down || true

      - name: Commit changes
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          git add .
          git commit -m "chore: update to Next.js ${{ steps.check_version.outputs.latest_version }}

          - Updated Next.js to ${{ steps.check_version.outputs.latest_version }}
          - Updated package versions to ${{ steps.check_version.outputs.latest_major_minor }}.0
          - Memory tests: ${{ steps.test_memory.outcome }}
          - Redis tests: ${{ steps.test_redis.outcome }}

          Co-Authored-By: GitHub Actions <github-actions[bot]@users.noreply.github.com>"

      - name: Push changes
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        run: |
          git push origin nextjs-${{ steps.check_version.outputs.latest_major_minor }}

      - name: Create Pull Request
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true'
        id: create_pr
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          MEMORY_STATUS="${{ steps.test_memory.outcome }}"
          REDIS_STATUS="${{ steps.test_redis.outcome }}"

          if [ "$MEMORY_STATUS" = "success" ] && [ "$REDIS_STATUS" = "success" ]; then
            PR_TITLE="✅ Next.js ${{ steps.check_version.outputs.latest_version }} - All tests passing"
            ALL_TESTS_PASSED="true"
            PR_BODY=$(cat <<'EOF'
          ## Next.js ${{ steps.check_version.outputs.latest_version }} Update

          All tests passing! This PR will be auto-merged and published to npm.

          ### Changes
          - ⬆️ Updated Next.js to ${{ steps.check_version.outputs.latest_version }}
          - 📦 Bumped package versions to ${{ steps.check_version.outputs.latest_major_minor }}.0

          ### Test Results
          - ✅ Memory cache tests: PASSED
          - ✅ Redis cache tests: PASSED

          ### Automation
          This PR will be automatically merged and a new version will be published to npm.

          ---
          *Auto-generated by [nextjs-version-check workflow](https://github.com/${{ github.repository }}/actions/workflows/nextjs-version-check.yml)*
          EOF
          )
          else
            PR_TITLE="⚠️ Next.js ${{ steps.check_version.outputs.latest_version }} - Tests need review"
            ALL_TESTS_PASSED="false"
            PR_BODY=$(cat <<'EOF'
          ## Next.js ${{ steps.check_version.outputs.latest_version }} Update

          Some tests failed. Manual review required before merging.

          ### Changes
          - ⬆️ Updated Next.js to ${{ steps.check_version.outputs.latest_version }}
          - 📦 Bumped package versions to ${{ steps.check_version.outputs.latest_major_minor }}.0

          ### Test Results
          - $( [ "$MEMORY_STATUS" = "success" ] && echo "✅" || echo "❌" ) Memory cache tests: $MEMORY_STATUS
          - $( [ "$REDIS_STATUS" = "success" ] && echo "✅" || echo "❌" ) Redis cache tests: $REDIS_STATUS

          ### Next Steps
          1. Review test failures
          2. Fix compatibility issues
          3. Re-run tests
          4. Merge when passing

          ---
          *Auto-generated by [nextjs-version-check workflow](https://github.com/${{ github.repository }}/actions/workflows/nextjs-version-check.yml)*
          EOF
          )
          fi

          PR_URL=$(gh pr create \
            --title "$PR_TITLE" \
            --body "$PR_BODY" \
            --base main \
            --head nextjs-${{ steps.check_version.outputs.latest_major_minor }})

          echo "pr_url=$PR_URL" >> $GITHUB_OUTPUT
          echo "all_tests_passed=$ALL_TESTS_PASSED" >> $GITHUB_OUTPUT

      - name: Auto-merge PR if all tests passed
        if: steps.check_version.outputs.needs_update == 'true' && steps.branch_check.outputs.branch_exists != 'true' && steps.test_memory.outcome == 'success' && steps.test_redis.outcome == 'success'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "All tests passed! Enabling auto-merge..."
          gh pr merge nextjs-${{ steps.check_version.outputs.latest_major_minor }} --auto --squash

          echo "✅ Auto-merge enabled for PR"
          echo "The PR will be merged once all status checks pass."
